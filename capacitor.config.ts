import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.thedevsaroj.com',
  appName: 'material-bootstrap',
  webDir: 'dist/clientapi/browser',
};

export default config;
